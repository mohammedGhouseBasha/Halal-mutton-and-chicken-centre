import {useState} from "react"; import {AdminNav} from "./Dashboard"; import {useOrders} from "../../context/OrderContext"; import {OrderStatus} from "../../types"; import {formatCurrency} from "../../utils/currency"; import {useToast} from "../../context/ToastContext";
const statuses:OrderStatus[]=["Pending","Confirmed","Preparing","Ready","Out for Delivery","Delivered","Cancelled"];
export default function AdminOrders(){const {orders,updateOrderStatus}=useOrders();const {showToast}=useToast();const [filter,setFilter]=useState("All");const shown=filter==="All"?orders:orders.filter(o=>o.status===filter);return <div className="admin"><AdminNav/><div className="admin-main"><header className="admin-header"><div><span className="eyebrow">ORDER MANAGEMENT</span><h1>Orders</h1></div><select className="admin-filter" value={filter} onChange={e=>setFilter(e.target.value)}><option>All</option>{statuses.map(s=><option key={s}>{s}</option>)}</select></header><div className="admin-panel table-wrap"><table><thead><tr><th>Order</th><th>Customer</th><th>Items</th><th>Amount</th><th>Payment</th><th>Status</th><th>Date</th></tr></thead><tbody>{shown.map(o=><tr key={o.id}><td data-label="Order"><b>{o.id}</b></td><td data-label="Customer">{o.customerName}<small>{o.phone}</small></td><td data-label="Items">{o.items.length}</td><td data-label="Amount">{formatCurrency(o.total)}</td><td data-label="Payment">{o.paymentMethod}</td><td data-label="Status"><select value={o.status} onChange={e=>{updateOrderStatus(o.id,e.target.value as OrderStatus);showToast("Order status updated")}}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td><td data-label="Date">{o.date}</td></tr>)}</tbody></table></div></div></div>}