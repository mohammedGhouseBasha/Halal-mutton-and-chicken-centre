import {useMemo,useState} from "react"; import {Search,SlidersHorizontal} from "lucide-react"; import {useProducts} from "../context/ProductContext"; import ProductGrid from "../components/ProductGrid";
export default function Products(){const {products:all}=useProducts();const [q,setQ]=useState("");const [cat,setCat]=useState("All");const cats=["All","Mutton","Chicken","Special Cuts","Offers"];const products=useMemo(()=>all.filter(p=>(cat==="All"||p.category===cat)&&(`${p.name} ${p.category} ${p.description}`).toLowerCase().includes(q.toLowerCase())),[all,q,cat]);return <div className="page"><div className="container"><div className="page-title"><span className="eyebrow">OUR CATALOGUE</span><h1>Fresh Meat Products</h1><p>Pick your cut, weight and preparation style.</p></div><div className="catalog-toolbar"><div className="search"><Search size={19}/><input aria-label="Search products" placeholder="Search products..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="filters"><SlidersHorizontal size={17}/>{cats.map(c=><button key={c} className={cat===c?"active":""} onClick={()=>setCat(c)}>{c}</button>)}</div></div><ProductGrid products={products}/></div></div>}