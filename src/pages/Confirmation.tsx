import {Link,useSearchParams} from "react-router-dom"; import {CheckCircle,MapPin,Clock,MessageCircle} from "lucide-react"; import {useOrders} from "../context/OrderContext"; import {formatCurrency} from "../utils/currency"; import {openWhatsApp,generateOrderNotificationMessage} from "../utils/whatsapp";
export default function Confirmation(){const [p]=useSearchParams();const {getOrder}=useOrders();const order=getOrder(p.get("order")||"");if(!order)return <div className="page"><div className="container empty"><h1>Order not found</h1><Link className="btn primary" to="/">Go Home</Link></div></div>;return <div className="page"><div className="container confirmation"><div className="success-icon"><CheckCircle size={50}/></div><span className="eyebrow">ORDER CONFIRMED</span><h1>Order Placed Successfully! 🎉</h1><p>Thanks, {order.customerName}. We’ve received your order.</p><div className="confirm-card"><div><span>Order ID</span><strong>#{order.id}</strong></div><div><span>Total</span><strong>{formatCurrency(order.total)}</strong></div><div><span>Payment</span><strong>{order.paymentMethod}</strong></div><div><span>Delivery</span><strong>{order.deliveryMethod}</strong></div></div><div className="delivery-note"><Clock/><div><b>Estimated delivery</b><span>30–60 minutes</span></div></div><h2>Ordered Products</h2>{order.items.map(i=><div className="confirm-item" key={i.id}><span>{i.name} · {i.weight<1?`${i.weight*1000}g`:`${i.weight}kg`} × {i.quantity}</span><b>{formatCurrency(i.pricePerKg*i.weight*i.quantity)}</b></div>)}<button type="button" className="btn outline full" onClick={()=>openWhatsApp(generateOrderNotificationMessage(order))}><MessageCircle size={16}/> Didn't open? Send order on WhatsApp</button><div className="confirm-actions"><Link className="btn primary" to={`/order-tracking?order=${order.id}`}>Track Order</Link><Link className="btn outline" to="/products">Continue Shopping</Link></div></div></div>}