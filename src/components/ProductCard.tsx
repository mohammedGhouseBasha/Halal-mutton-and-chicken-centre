import {Link} from "react-router-dom"; import {ShoppingCart,Eye} from "lucide-react"; import {Product} from "../types"; import {formatCurrency} from "../utils/currency"; import {Button,Badge} from "./ui"; import {useCart} from "../context/CartContext";
export default function ProductCard({product}:{product:Product}){const {addToCart}=useCart();return <article className="product-card"><Link to={`/products/${product.id}`} className="product-img"><img src={product.image} alt={product.name}/>{!product.available&&<span className="sold">Out of Stock</span>}</Link><div className="product-body"><div className="row"><Badge>{product.category}</Badge><span className="stock">{product.available?`${product.stockKg}kg available`:"Out of Stock"}</span></div><Link to={`/products/${product.id}`}><h3>{product.name}</h3></Link><p>{product.description}</p><div className="price">{formatCurrency(product.pricePerKg)} <small>/ kg</small></div><div className="card-actions"><Button disabled={!product.available} onClick={()=>addToCart(product,1,"Curry Cut")}><ShoppingCart size={16}/> Add to Cart</Button><Link className="btn outline" to={`/products/${product.id}`}><Eye size={16}/> Details</Link></div></div></article>}